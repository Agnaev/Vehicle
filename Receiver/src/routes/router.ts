import main from './main_routes';
import types from './sensors_router';
import values from './sensors_values';
import states from './states_router';

export default {
    main,
    types,
    values,
    states
}