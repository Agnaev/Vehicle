import main from './main_routes';
import types from './metrics_router';
import values from './metrics_values';
import partials from './partials_router';

export default {
    main,
    types,
    values,
    partials
}